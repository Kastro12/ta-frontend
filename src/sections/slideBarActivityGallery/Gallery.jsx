'use client';
import SlideBarActivityGallery from './SlideBarActivityGallery';

const Gallery = ({ serverData }) => {
  return <SlideBarActivityGallery serverData={serverData} />;
};

export default Gallery;
